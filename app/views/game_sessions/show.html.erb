<% if (DateTime.now < @game_session.ending_at) %>
  <%= render 'landing', game_session: @game_session, seance: @seance %>
<% else %>
  <%= render 'results', game_session: @game_session, players_ordered: @players_ordered %>
<% end %>

